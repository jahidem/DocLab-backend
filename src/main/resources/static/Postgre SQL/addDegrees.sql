insert
    into
        degree(name)
    values('MBBS');

insert
    into
        degree(name)
    values('BDS');

insert
    into
        degree(name)
    values('BAMS');

insert
    into
        degree(name)
    values('BAMS');

insert
    into
        degree(name)
    values('BHMS');

insert
    into
        degree(name)
    values('BYNS');

insert
    into
        degree(name)
    values('B.V.Sc & AH');

insert
    into
        degree(name)
    values('MS');


select * from "degree"  ;